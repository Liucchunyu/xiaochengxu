function getNights(startDate,endDate) {
	var nights = Math.abs(getDate(endDate).getTime() - getDate(startDate).getTime()) / (1000 * 60 * 60 * 24).toFixed(0);
	return nights;
}

module.exports = {
	getNights:getNights
}
